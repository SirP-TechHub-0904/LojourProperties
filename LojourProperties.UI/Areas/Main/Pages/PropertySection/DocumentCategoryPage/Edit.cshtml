@page
@model LojourProperties.UI.Areas.Main.Pages.PropertySection.DocumentCategoryPage.EditModel

@{
    ViewData["Title"] = "Edit";
}

<div class="card card-default">
    <div class="card-header">
        <h3 class="card-title">UPDATE DOCUMENT CATEGORY </h3>


    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <div class="row">
            <div class="col-md-12">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="DocumentCategory.Id" />
            <div class="form-group">
                <label asp-for="DocumentCategory.Name" class="control-label"></label>
                <input asp-for="DocumentCategory.Name" class="form-control" />
                <span asp-validation-for="DocumentCategory.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
